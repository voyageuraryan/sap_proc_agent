apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: proc-agent
resources:
  - 00-namespace.yaml
  - 10-config.yaml
  - 20-erp.yaml
  - 30-review.yaml
  - 40-evals.yaml
commonLabels:
  app.kubernetes.io/part-of: sap-proc-agent
images:
  - name: sap-proc-agent
    newTag: local
